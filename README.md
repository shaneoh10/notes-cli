# Go CLI Note Taking App

A simple and lightweight CLI note-taking application built with Go. Create notes directly from the terminal, display them in a clean format, and automatically save them as JSON files with timestamps for easy organisation and storage.
